/* Entry config for the public (logged-out) bundle — mirrors tsconfig.viewer.json. */
{
  "extends": "./tsconfig.json",
  "compilerOptions": {
    "outDir": "./out-tsc/public",
    "types": []
  },
  "files": ["src/public-app/main.ts"],
  "include": ["src/public-app/**/*.d.ts"]
}
